name: Integrate Availability Feature
on:
  push:
    branches: [main]
jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Integrate
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('profile.html')
          s=p.read_text()
          tag='<script src="availability.js"></script>'
          if tag not in s:
              s=s.replace('</body>',tag+'</body>')
              p.write_text(s)
          PY
      - name: Commit integration and remove workflow
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add profile.html
          git commit -m "Integrate availability into profile" || true
          git rm .github/workflows/integrate-availability.yml || true
          git commit -m "Remove temporary availability workflow" || true
          git push